hbp.hello.js
============

Provides a module to let hello.js authenticate a user using the
Human Brain Project account.

Run example
-----------

```
$ npm install -g bower
$ npm install
$ bower install
$ gulp serve
```
